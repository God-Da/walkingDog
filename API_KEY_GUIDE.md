# 🔑 API 키 설정 가이드

## 공공데이터포탈 API 키 설정

### ⚠️ 중요: 하나의 키로 모든 API 사용!

공공데이터포탈에서는 **하나의 일반 인증키(Encoding)**로 모든 API를 사용할 수 있습니다.
- 에어코리아 API
- 기상청 API
- 기타 모든 공공데이터포탈 API

**각 API마다 별도의 키가 아닙니다!**

### 설정 방법

1. [공공데이터포탈](https://www.data.go.kr/) 접속
2. 로그인 후 **마이페이지** > **개발계정** 이동
3. **일반 인증키(Encoding)** 복사
   - 이 키 하나로 에어코리아와 기상청 API 모두 사용 가능합니다
   - 인증키 발급현황에서 복사한 키를 사용하시면 됩니다

4. `backend/src/main/resources/application.properties` 파일에 설정:

```properties
public.data.api.key=여기에_복사한_일반_인증키_입력
```

또는 환경변수로 설정:

```bash
# Windows (PowerShell)
$env:PUBLIC_DATA_API_KEY="your-api-key-here"

# Windows (CMD)
set PUBLIC_DATA_API_KEY=your-api-key-here

# Linux/Mac
export PUBLIC_DATA_API_KEY=your-api-key-here
```

### API 신청 확인

사용하려는 API를 신청했는지 확인하세요:
- ✅ 에어코리아 대기오염정보 조회 서비스
- ✅ 기상청 단기예보 조회 서비스

## 카카오맵 API 키 설정

1. [카카오 개발자 콘솔](https://developers.kakao.com/) 접속
2. 내 애플리케이션 > 애플리케이션 추가하기
3. **JavaScript 키** 복사

4. `frontend/public/index.html` 파일 수정:

```html
<script type="text/javascript" src="//dapi.kakao.com/v2/maps/sdk.js?appkey=여기에_JavaScript_키_입력&autoload=false"></script>
```

5. `frontend/src/App.js` 파일도 수정 (19번째 줄):

```javascript
script.src = `//dapi.kakao.com/v2/maps/sdk.js?appkey=여기에_JavaScript_키_입력&autoload=false`;
```

## 확인 사항

✅ 공공데이터포탈 일반 인증키(Encoding) 설정 완료  
✅ 카카오맵 JavaScript 키 설정 완료  
✅ 에어코리아 API 신청 완료  
✅ 기상청 API 신청 완료  

이제 애플리케이션을 실행할 수 있습니다!


